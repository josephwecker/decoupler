ctags - make sure updated (project)

set pwd (for ctags) (project)

set makeprg=./waf (project)

:compiler gcc   (project)

manpages   "K"  (global-personal - modified width)

------------------

gdb:  pyclewn

autocomplete?

signatures?

git-grep / search-include-path?

profiler : VTune / CodeAnalyst / oprofile

strace
valgrind (callgrind / kcachegrind)

lint
